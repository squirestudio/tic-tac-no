import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.squirestudio.ticattacktoe',
  appName: 'Tic Attack Toe',
  webDir: 'out'
};

export default config;
